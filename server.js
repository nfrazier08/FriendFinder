// server.js file should require the basic npm packages class: `express`, `body-parser` and `path`.
var express = require("express");
var bodyParser = require("body-parser");
var path = require("path");

